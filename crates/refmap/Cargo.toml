[package]
name = "kast-refmap"
version = "0.1.0"
edition.workspace = true
description = "map where keys are references - not values behind references"
license.workspace = true
repository.workspace = true

[dependencies]
