const fib = [T] (n :: T) -> T => (
    if less[T](n, 2) then (
        1
    ) else (
        add[T](fib[T](sub[T](n, 1)), fib[T](sub[T](n, 2)))
    )
);

const TEN :: Int32 = 10;

const main = () -> () => (
    print_String("fib(10) = ");
    print_Int32(fib[Int32](TEN));
    let arr :: List[Int] = [ 1, 2, 3, 4, 5 ];
    print_String("\n");
);
