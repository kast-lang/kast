const main = () => (
    let hello = "Hello\n";
    @delimited_continuation delim (
        let f = move () => (
            @capture_continuation delim k ();
        );
        f();
        free_closure[()](f);
    );
);
